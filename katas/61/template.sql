CREATE TABLE users (
    id INT PRIMARY KEY,
    name VARCHAR(100),
    email VARCHAR(255),
    created_at TIMESTAMP
);

-- TODO: Insert 5 users with incrementing IDs
-- Use Vim's input mode and increment functionality to generate INSERT statements 